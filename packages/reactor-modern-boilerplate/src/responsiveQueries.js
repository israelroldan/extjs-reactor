// Keys for the responsiveConfig prop.  
// Keeping them in one place ensure size boundaries are consistent across components.
export const small = 'width <= 600';
export const notSmall = 'width > 600';
export const medium = 'width <= 1024';
export const large = 'width > 1024';