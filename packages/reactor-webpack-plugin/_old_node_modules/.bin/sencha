#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@extjs/sencha-cmd/bin/sencha" "$@"
  ret=$?
else 
  node  "$basedir/../@extjs/sencha-cmd/bin/sencha" "$@"
  ret=$?
fi
exit $ret
