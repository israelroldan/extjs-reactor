{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "increment": "lerna publish --skip-git --skip-npm",
    "clean": "lerna clean",
    "install:next": "npm install && node scripts/next.js"
  },
  "devDependencies": {
    "lerna": "^2.0.0"
  }
}
